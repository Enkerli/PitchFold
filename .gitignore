build/
build-ios/
build-macos/
/JUCE
*.xcodeproj/
DerivedData/

# WebUI source now lives in the music-suite monorepo (apps/pitchfold); only the
# C++ editor (PitchFoldEditor.*) remains under Source/WebUI here. The esbuild
# bundle is generated into the build tree from the monorepo source.
Source/WebUI/node_modules/
Source/WebUI/dist/
webui.local.cmake

# macOS
.DS_Store
**/.DS_Store

# Editor
.vscode/
*.swp
