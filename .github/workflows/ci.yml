# Cross-platform build matrix (macOS · Linux · Windows) via the shared
# enkerli-juce reusable workflow. Proves every desktop version compiles:
# macOS AU/VST3/CLAP, Windows VST3/CLAP, Linux LV2/CLAP, Standalone everywhere.
name: build
on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: Enkerli/enkerli-juce/.github/workflows/build-plugin.yml@main
